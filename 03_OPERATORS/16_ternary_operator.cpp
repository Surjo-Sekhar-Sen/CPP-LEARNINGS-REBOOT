#include <iostream>
using namespace std;

int main(){
    // int age = 19;
    // (age > 18) ? cout << "Can Vote!!" : cout << "Cannot Vote!!";

    int x = 10, y = 20;
    (x > y) ? cout << "x is greater than y" : cout << "y is greater than x";
    return 0;
}